import { HungarianDateFormatPipe } from './hungarian-date-format.pipe';

describe('HungarianDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new HungarianDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
